header kann schöner generiert werden, viel code dupl,

electiondescription soll eig nicht über cbmc wissen

signatur im hintergrund nicht dasselbe, als param kommen noch amt_votes hinzu, aber sollte nach außen hin nicht gezeigt werden

V,C, etc namen änderbar, unifiednamecontainer

split: teilt array in beliebige anzahl andere arrays (arr0,..,arrn) <- split(arr)

code generation: was abh von input und output type?

vergleiche

votesum



komplettes refactoring ok?