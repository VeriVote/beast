package edu.pse.beast.propertylist.View;

import static org.junit.Assert.*;

import org.junit.After;
import org.junit.AfterClass;
import org.junit.Before;
import org.junit.BeforeClass;
import org.junit.Test;



public class PresentResultTest {
	/*
	PLModelInterface model = new PLModel();
	PLControllerInterface controller = new PropertyList(model);
	

	@BeforeClass
	public static void setUpBeforeClass() throws Exception {
	}

	@AfterClass
	public static void tearDownAfterClass() throws Exception {
	}

	@Before
	public void setUp() throws Exception {
		PropertyListWindowStarter starter = new PropertyListWindowStarter(controller, model);
		starter.start();
		controller.addNewProperty();
		
		ResultInterface res = new InterfaceTest();
		
		controller.presentResult(res);
	}

	@After
	public void tearDown() throws Exception {
	}

	@Test
	public void testForNextToPresentObject() {
		assertNotNull(((PropertyList) controller).getView().getNextToPresent());
	}
	
	@Test
	public void testForResultType() {
		assert(((PropertyList) controller).getView().getNextToPresent().getPropertyItem().getResultType() == ResultType.TESTED);
	}
	
	
public static void main(String[] args) {
		
		PLModelInterface model = new PLModel();
		PLControllerInterface controller = new PropertyList(model);
		
		PropertyListWindowStarter starter = new PropertyListWindowStarter(controller, model);
		starter.start();
		controller.addNewProperty();
		
		ResultInterface res = new InterfaceTest();
		
		controller.presentResult(res);
    }
*/
}
